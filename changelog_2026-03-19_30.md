# ประวัติการปรับปรุงแก้ไข (ช่วง 19–30 มีนาคม 2026)

> เอกสารนี้สรุปจาก **git history (commit จริง)** และ **การแก้ไขใน working tree ที่ยังไม่ commit (git diff)**

---

## 1) ภาพรวมสถานะ Git ณ วันที่ตรวจสอบ

จาก `git status -sb`

- **Branch:** `main...origin/main [ahead 10]`
  - เครื่องนี้มี commit ที่ยังไม่ได้ push ขึ้น remote จำนวน 10 commit
- **ไฟล์ที่แก้ไข แต่ยังไม่ commit:**
  - `.clasp.json`
  - `.clasp.json.backup`
  - `Installer.js`
  - `Scoring.js`
  - `SelfTest.js`
  - `deleteStudent.html`
  - `new_settings_editor.html`
- **ไฟล์ใหม่ (untracked):**
  - `push.bat`

สรุป: ช่วงปลายเดือน (ประมาณ 28–30 มี.ค.) มีการแก้ไฟล์จริงในเครื่อง แต่ยังไม่ commit จึงไม่ปรากฏใน `git log`

---

## 2) รายการปรับปรุงที่เป็น Commit จริง (ช่วง 19–21 มี.ค.)

> อ้างอิงจาก `git log --all --since "2026-03-19" --until "2026-03-31"` และ `git reflog` (พบต่อเนื่องถึง 21 มี.ค.)

### 2.1 งานด้าน Template/Export

- เพิ่มแนวทางสร้าง template แบบปลอดภัย: copy-then-clean (`createCleanTemplate`)
- ปรับ export ใช้ลิงก์ `/copy` เพื่อรักษา bound scripts
- Cleanup ชีตที่ไม่ควรติดไปในไฟล์ export (score/backup/attendance/profile)

ไฟล์ที่พบบ่อยใน log:
- `Installer.js`
- `spa_main_menu.html`

### 2.2 งานด้านคะแนน/คลังคะแนน (Scoring/Warehouse)

- เพิ่ม validation + safe write ไปที่ warehouse (ลดโอกาสข้อมูลหาย/ทับ)
- รองรับ layout ชีตคะแนนหลายแบบด้วย `detectSheetLayout_`
- ปรับ locking / stale data detection / optimize การอัปเดตระดับแถวใน warehouse

ไฟล์ที่พบบ่อยใน log:
- `Scoring.js`
- `score_table_viewer.html` (และมี `score.html` โผล่ใน commit บางตัว)

### 2.3 งานด้านการมาเรียน (Attendance)

- ปรับ performance หน้า monthly attendance (ลด fetch ซ้ำ, ทำงาน parallel)
- เพิ่ม/ปรับการแสดง “วันเรียน”, แก้ markAll/clearAll, sort รหัสนักเรียนแบบ numeric

ไฟล์ที่พบบ่อยใน log:
- `Attendance.js`
- `attendance_monthly.html`
- `semester_attendance_summary.html`

### 2.4 งานด้านรายงาน/เกรด/one page report

- ปรับ UI one page report ให้เลือก `ชั้น/ห้อง` แบบ dropdown เดียว + เลือกภาคเรียน และลดจำนวนการเรียก server
- แก้การแสดงผลเกรด (เช่น 3.5 ไม่ถูกปัดเป็น 4)

ไฟล์ที่พบบ่อยใน log:
- `one_page_report.html`
- `OnePageReport.js`
- `ReportGenerator.js`

### 2.5 งานด้าน ปพ.6 (PP6)

- ปรับ layout ให้กระชับขึ้น, การเรียงวิชา, การแสดงชื่อครู/ข้อมูลนักเรียน และการเรียก generate PDF

ไฟล์ที่พบบ่อยใน log:
- `pp6.js`

---

## 3) งานช่วง 28–30 มี.ค. (ยังไม่ commit) — สรุปจาก git diff

> อ้างอิงจาก `git diff --stat` และ `git diff -- <file>`

### 3.1 สรุปจำนวนบรรทัดที่เปลี่ยน

- **7 files changed**
- **เพิ่ม 64 บรรทัด / ลบ 28 บรรทัด**

ไฟล์ที่เปลี่ยน:
- `.clasp.json`
- `.clasp.json.backup`
- `Installer.js`
- `Scoring.js`
- `SelfTest.js`
- `deleteStudent.html`
- `new_settings_editor.html`

### 3.2 รายละเอียดตามไฟล์

#### Installer.js

- **ระบบจำหน่ายนักเรียน:**
  - เปลี่ยนจากจำหน่ายเฉพาะ `ป.6` → รองรับ **`ป.6` และ `ม.3`**
  - rename ฟังก์ชัน:
    - `graduateP6Students_()` → `graduateStudents_()`
    - `graduateP6()` → `graduateStudents()`
  - ปรับข้อความ log/ข้อความผลลัพธ์ให้สอดคล้อง “ป.6/ม.3”

- **Preview สลับปีการศึกษา:**
  - ขยาย `gradeCounts` ให้ครอบคลุม **`อ.1–ม.3`**
  - เพิ่ม `graduateCount = ป.6 + ม.3`
  - คง `p6Count` เป็น alias เพื่อ compatibility ของ UI เดิม
  - ปรับ `promoteCount = studentCount - graduateCount`

#### new_settings_editor.html

- ปรับข้อความยืนยัน/ข้อความผลลัพธ์ให้ตรงกับ flow ใหม่:
  - จำหน่าย **ป.6 และ ม.3**
  - เลื่อนชั้น **อ.1→อ.2 … ม.2→ม.3**
  - ปรับข้อความสรุปเป็น “จำหน่าย ป.6/ม.3”

#### deleteStudent.html

- เพิ่มตัวเลือกใน dropdown “ระดับชั้น” ให้มีครบ:
  - `อ.1, อ.2, อ.3`
  - `ม.1, ม.2, ม.3`

#### Scoring.js

- ปรับให้ debug อ่านคลังคะแนนแบบรองรับปีการศึกษา:
  - `getSheetByName('SCORES_WAREHOUSE')`
  - → ใช้ `S_getYearlySheet('SCORES_WAREHOUSE')` ถ้ามีฟังก์ชัน (ไม่งั้น fallback)

---

## 4) สรุปรวม (19–30 มี.ค.)

- **19–21 มี.ค.:** มี commit จริงใน git (Template export/คะแนน/มาเรียน/รายงาน/PP6)
- **28–30 มี.ค.:** มีการแก้ระบบ “สลับปีการศึกษา 2569” และ UI บางหน้าในเครื่องจริง แต่ยังไม่ commit (ยืนยันจาก git diff)
