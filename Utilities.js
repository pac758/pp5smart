// ============================================================
// 🛠️ IX. UTILITY & HELPER FUNCTIONS
// ============================================================

/**
 * แปลงวันที่เป็นรูปแบบ "วัน เดือน พ.ศ." (เช่น "24 มีนาคม 2565")
 */

/**
 * แปลงวันที่เป็นรูปแบบสำหรับ input date 'YYYY-MM-DD'
 */

/**
 * แปลงระดับชั้นแบบย่อเป็นชื่อเต็ม (เช่น "ป.1" -> "ประถมศึกษาปีที่ 1")
 */
function convertGradeToFullName(grade) {
  const gradeMap = {
    "ป.1": "ประถมศึกษาปีที่ 1", "ป.2": "ประถมศึกษาปีที่ 2", "ป.3": "ประถมศึกษาปีที่ 3",
    "ป.4": "ประถมศึกษาปีที่ 4", "ป.5": "ประถมศึกษาปีที่ 5", "ป.6": "ประถมศึกษาปีที่ 6",
    "อ.1": "อนุบาล 1", "อ.2": "อนุบาล 2", "อ.3": "อนุบาล 3",
  };
  return gradeMap[grade] || grade;
}
/**
 * ล้างข้อมูลที่เก็บไว้ใน CacheService ฝั่ง Server
 * --------------------------------
 * ฟังก์ชันนี้จะถูกเรียกจาก JavaScript ฝั่ง Client (ปุ่ม 'ล้าง Cache')
 * เพื่อล้างข้อมูลสรุปผลต่างๆ ที่เก็บไว้ชั่วคราว
 * ทำให้การโหลดข้อมูลครั้งถัดไปเป็นข้อมูลใหม่ล่าสุดจาก Sheet โดยตรง
 */
